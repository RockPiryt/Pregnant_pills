apiVersion: v1
kind: Service
metadata:
  name: preg-nutri-svc
spec:
  selector:
    app: preg-nutri
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
