apiVersion: apps/v1
kind: Deployment
metadata:
  name: preg-pills-app
spec:
  replicas: 2
  template:
    spec:
      nodeSelector:
        node-role: worker
      containers:
        - name: preg-pills-container
          image: 340507401402.dkr.ecr.eu-west-1.amazonaws.com/deploy-preg-app:preg-pills-app-latest
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "300Mi"