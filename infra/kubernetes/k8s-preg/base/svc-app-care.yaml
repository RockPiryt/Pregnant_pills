apiVersion: v1
kind: Service
metadata:
  name: preg-care-svc
spec:
  selector:
    app: preg-care
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
