apiVersion: apps/v1
kind: Deployment
metadata:
  name: preg-baby-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: preg-baby
  template:
    metadata:
      labels:
        app: preg-baby
    spec:
      containers:
        - name: preg-baby-container
          image: rockpiryt/preg-baby-app:latest
          
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          
          ports:
            - containerPort: 8080
          
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          
          env:
            - name: APP_ENV
              valueFrom:
                configMapKeyRef:
                  name: preg-baby-config
                  key: APP_ENV

            - name: FLASK_APP
              valueFrom:
                configMapKeyRef:
                  name: preg-baby-config
                  key: FLASK_APP

            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: preg-baby-secrets
                  key: SECRET_KEY

            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: preg-baby-secrets
                  key: DATABASE_URL


