apiVersion: apps/v1
kind: Deployment
metadata:
  name: preg-org-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: preg-org
  template:
    metadata:
      labels:
        app: preg-org
    spec:
      containers:
        - name: preg-org-container
          image: rockpiryt/preg-org-app:latest
          
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          
          ports:
            - containerPort: 8080
          
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          
          env:
            - name: APP_ENV
              valueFrom:
                configMapKeyRef:
                  name: preg-org-config
                  key: APP_ENV

            - name: FLASK_APP
              valueFrom:
                configMapKeyRef:
                  name: preg-org-config
                  key: FLASK_APP

            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: preg-org-secrets
                  key: SECRET_KEY

            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: preg-org-secrets
                  key: DATABASE_URL


