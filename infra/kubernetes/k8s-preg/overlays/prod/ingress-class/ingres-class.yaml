# Assigne ingress controller to ingress
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: prod-preg-app-aws-ingress-class
  annotations:
    ingressclass.kubernetes.io/is-default-class: "true"
spec:
  controller: ingress.k8s.aws/alb