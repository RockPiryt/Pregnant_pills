apiVersion: apps/v1
kind: Deployment
metadata:
  name: preg-pills-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: preg-pills
  template:
    metadata:
      labels:
        app: preg-pills
    spec:
      containers:
        - name: preg-pills-container
          image: rockpiryt/preg-pills-app:prod

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"

          ports:
            - name: http
              containerPort: 8080

          env:
            - name: APP_ENV
              valueFrom:
                configMapKeyRef:
                  name: preg-app-config
                  key: APP_ENV

            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: preg-app-secrets
                  key: SECRET_KEY

            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: preg-app-secrets
                  key: DATABASE_URL

