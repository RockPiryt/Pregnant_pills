apiVersion: batch/v1
kind: Job
metadata:
  name: test-db-reset
  namespace: preg-test
spec:
  backoffLimit: 2
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: db-reset
          image: rockpiryt/pregnant-pills:latest
          imagePullPolicy: Always
          env:
            - name: APP_ENV
              valueFrom:
                configMapKeyRef:
                  name: test-pregnant-pills-config
                  key: APP_ENV

            - name: FLASK_APP
              valueFrom:
                configMapKeyRef:
                  name: test-pregnant-pills-config
                  key: FLASK_APP

            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: test-pregnant-pills-secrets
                  key: DATABASE_URL

          volumeMounts:
            - name: reset-sql
              mountPath: /reset
              readOnly: true
            - name: reset-script
              mountPath: /scripts
              readOnly: true

          command: ["sh", "-c"]
          args:
            - |
              chmod +x /scripts/db-reset.sh
              /scripts/db-reset.sh

      volumes:
        - name: reset-sql
          configMap:
            name: test-postgres-reset-sql
        - name: reset-script
          configMap:
            name: test-postgres-reset-script
